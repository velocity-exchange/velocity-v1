---
'@velocity-exchange/admin-cli': minor
---

Add `program upgrade` command: propose a BPFLoaderUpgradeable upgrade from an existing on-chain buffer (validates the buffer and prints its executable hash before proposing).
