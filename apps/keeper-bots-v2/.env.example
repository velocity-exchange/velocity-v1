# The keeper's signing key, as a JSON byte array (e.g. the contents of a
# solana-keygen file). Do not commit a real key.
KEEPER_PRIVATE_KEY=[1,2,3,...,64]
ENDPOINT=https://api.devnet.solana.com
ENV=devnet
