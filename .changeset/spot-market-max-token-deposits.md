---
'@velocity-exchange/admin-cli': minor
---

Add `spot-market set-max-token-deposits` command: update a spot market's hard deposit cap (raw token base units, 0 = uncapped) as warm/cold admin, with the usual multisig proposal routing.
