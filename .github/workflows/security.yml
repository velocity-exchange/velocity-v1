# Temporarily disabled while we evaluate the Claude security review action.
# Restore by removing the leading "# " from every line below.

# name: Security Scans

# on:
#   push:
#     branches:
#       - master
#   pull_request:
#     branches:
#       - master

# jobs:
#   monitor:
#     name: Sync with snyk.io
#     runs-on: ubicloud
#     steps:
#       - uses: actions/checkout@61b9e3751b92087fd0b06925ba6dd6314e06f089 # master
#       - name: Monitor on snyk.io
#         uses: snyk/actions/node@9adf32b1121593767fc3c057af55b55db032dc04 # master
#         env:
#           SNYK_TOKEN: ${{ secrets.SNYK_TOKEN }}
#         with:
#           command: monitor --org=${{ secrets.SNYK_ORG }} --all-projects --project-business-criticality=critical --project-lifecycle=production --project-environment=backend
#   sast:
#     needs: monitor
#     name: Code Scan
#     runs-on: ubicloud
#     steps:
#       - name: Checkout changes
#         uses: actions/checkout@ee0669bd1cc54295c223e0bb666b733df41de1c5 # v2

#       - uses: snyk/actions/setup@9adf32b1121593767fc3c057af55b55db032dc04 # master
#       - name: Snyk Code Scan
#         run: snyk code test --org=${{ secrets.SNYK_ORG }} --severity-threshold=medium --sarif-file-output=snyk-sast.json
#         env:
#            SNYK_TOKEN: ${{ secrets.SNYK_TOKEN }}

#       - name: Upload code report
#         if: always()
#         uses: github/codeql-action/upload-sarif@231aa2c8a89117b126725a0e11897209b7118144 # v1
#         with:
#           sarif_file: snyk-sast.json
#   sca:
#       needs: monitor
#       name: Dependency Scan
#       runs-on: ubicloud
#       steps:
#         - name: Checkout changes
#           uses: actions/checkout@ee0669bd1cc54295c223e0bb666b733df41de1c5 # v2

#         - name: Snyk Dependency Scan
#           uses: snyk/actions/node@9adf32b1121593767fc3c057af55b55db032dc04 # master
#           env:
#             SNYK_TOKEN: ${{ secrets.SNYK_TOKEN }}
#           with:
#             args: --all-projects --org=${{ secrets.SNYK_ORG }} --severity-threshold=medium --fail-on=upgradable --sarif-file-output=snyk-sca.sarif

#         - name: Upload dependency report
#           if: always()
#           uses: github/codeql-action/upload-sarif@231aa2c8a89117b126725a0e11897209b7118144 # v1
#           with:
#             sarif_file: snyk-sca.sarif
