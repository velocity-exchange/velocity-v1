# PropAMM + Order Flow — Implementation Plan

Living doc. Design source of truth: [PropAMM + Order Flow Design (NEW)](https://app.notion.com/p/3aa9833913f38099a30fc395fabb1d26) — if this doc and the Notion disagree, the Notion wins.
Last synced with Notion: **2026-07-28**.

## How to use

- `[ ]` todo, `[~]` in progress, `[x]` done.
- When the Notion changes: diff it against the Design Snapshot below, update the snapshot and any affected tasks, add a Sync Log row, bump the sync date above.

## Sync log

| Date       | Notion change                    | Plan impact |
| ---------- | -------------------------------- | ----------- |
| 2026-07-27 | Initial plan authored from design | —           |
| 2026-07-27 | Quoter CPIs pass is_signer:false; attestation verified by instructions-sysvar introspection | S1 updated; prop_amm.rs fixed (was forwarding outer-tx signer status — a quoter could have signed for the taker's wallet) |
| 2026-07-27 | — (implementation) | CLOB: flow_authority removed (velocity dictates activation delay incl. 0 — attestation policy is router-fill policy); arena converted to `Slab<ClobHeaderV0, OrderNodeV0>` — per-market capacity from account size at creation, growable via realloc (Phoenix v1 tiers: 512–4096/side) |
| 2026-07-27 | — (implementation) | CLOB migrated to Anchor v2 (anchor-next alpha, otter-sec/anchor pinned rev 4fbe613); response buffer folded into the market account tail + ResponsePointerV0; all 9 litesvm tests green |
| 2026-07-27 | Standardized on clamp-at-fill + notional counter (option 2); consistency-rule paragraph removed from User Orders | None — plan S3/S5 and the built CLOB already match |
| 2026-07-27 | Quoter responses moved from return data to a per-quoter response account + pointer | S1 updated; CLOB quote_v0/execute_v0 gain a response account; caps become heap/CU bounds, not ABI bounds |
| 2026-07-27 | — (plan-side decision)           | Anchor v2 programs get their own workspace (`anchor-v2/`); `.so` fixture build feeds velocity-workspace litesvm tests |
| 2026-07-28 | Review comment: OrderNode needs a min-size predicate for the cancel crank (partial fills leave sub-min remainders) | S2/S3 updated; execute now culls sub-`min_order_size` remainders in place (`meets_min_order_size` helper on OrderNodeV0); crank predicate extended to orders stranded below a raised min |
| 2026-07-28 | Account-set race decided: quote/execute take the loaded-user set; absent-user orders skipped within a grace window, `StaleUserSet` past it; deferred settlement rejected (maker free option); speed bump may go to 0, so grace — not activation delay — carries the anti-grief load | S1/S2 updated; CLOB implements `users` + `placed_slot` + `unknown_user_grace_slots`; Phase 2 router passes loaded users + rejects unloaded-user balance changes; keep-rs over-allocation + maker ALT noted |
| 2026-07-28 | CLOB margin decided: keep the DLOB model — gate at placement, count resting orders via worst-case `open_bids`/`open_asks`; `open_order_notional` + margin-floor scheme dropped; `quoting_reserve` is PropAMM-only. Trigger-time gate mirrors `trigger_order` (margin-fail → cancel `InsufficientFreeCollateral`, no re-arm) | S3/S5 updated; Phase 2 adapter + trigger + test tasks reworded |
| 2026-07-28 | Evict/expiry made velocity-mediated so aggregates are exact: inline tail-evict replaced by soft-cap crank (`evict_worst_v0`, hard cap rejects all placements), expiry skip-only in execute + `remove_expired_v0` crank (mirrors today's keeper-cranked DLOB expiry), execute-response carries sub-min culls; reconcile ix dropped; trigger re-arm becomes eager | S2/S3/S5 updated; CLOB implements it (12 tests green); Phase 2 crank-wrapper task added |
| 2026-07-28 | `quoting_reserve` dropped — vestige of the margin floor; routers read the PropAMM user's account and margin-truncate depth (already the Risk Assessment mechanism), fill-time clamp enforces | S3 updated; Phase 2/3 tasks reworded |
| 2026-07-28 | No new unbacked-order crank: the existing `force_cancel_orders` keeper ix extends to CLOB orders; stranded-below-raised-min sweep dropped (those orders are margin-backed and just rest) | S3 updated; Phase 2 task reworded; unused `meets_min_order_size` helper removed from the CLOB |
| 2026-07-28 | — (implementation) | Phase 2 started: `integration-tests/` litesvm workspace (velocity.so + clob.so fixtures) + `QuoterV0` registry live in velocity (5 ixs, IDL regenerated, fuzz IDLs synced, 932 unit tests + registry lifecycle test green). `QuoterV0` gains an `authority` field |
| 2026-07-28 | Registry roles corrected: creation is consent (Custom entries created by the quoted `User.authority`; separate approve ix deleted), `is_active` = maker's permanent kill switch (authority-gated, no handoff), `is_approved` = admin vetting, reset by config changes; quote also gates on both flags | Registry ixs reworked (`update_quoter_approved` replaces `approve_quoter`); IDL regenerated; mirrors updated |
| 2026-07-28 | Quoter CPI signing decided by doing: velocity `invoke_signed`s as its signer PDA (never forwards outer signers) — a quoter registers the velocity signer in its execute account list to authenticate the caller; CLOB `place_authority` = velocity signer | `invoke_quoter` reworked (signer PDA + program account info); `probe_quoter` test ix + cross-program probe test; CU recorded in the Phase 2 task |
| 2026-07-28 | — (implementation) | S6 split math implemented + 3-quoter `probe_router` end to end (waterfall asserted against real CLOB fills, at-or-better enforced); CLAUDE.md gains a Rust style rule (declarative iterators where perf-equivalent; hoist loop invariants) |
| 2026-07-28 | Priority generalized to an admin-assigned number (S6): vAMM outranks CLOB outranks customs; pro rata within a tier; defaults by type with gaps (0/10/20) | `QuoterV0.priority` + `update_quoter_priority` admin ix; split reworked to priority tiers (simpler — CLOB-first special case dissolved into tier pro rata); mirrors updated |
| 2026-07-28 | Fill-path collapse designed from a full audit of `fill_perp_order`: AMM-JIT (jit.rs, intensity, allowed-threading) ≡ degenerate last-look shading and dies; method determination + step loop + scratch projection + maker-price freeze all collapse into one router pass; pre-flight/post-loop/settle bodies survive | Roadmap in the Phase 2 rewrite task; `router_take` gained taker-limit book truncation (+ test) |
| 2026-07-28 | — (implementation) | Controller half of the fill-path rewrite landed: `fulfill_perp_order` gains an `external_books` router branch (`fulfill_perp_order_router_pass` — one pass over maker books + vAMM ladder, settled through the existing fee-policy bodies), post-loop checks extracted to `fulfill_perp_order_post_checks` shared by both paths, `fill_perp_order_with_router` threads books from the (future) ix layer; legacy step loop untouched; unit test fills a taker across a DLOB maker + the vAMM ladder through the router pass (949 tests green) |
| 2026-07-28 | — (implementation) | Router path made reachable: new `fill_perp_order_router` keeper ix (same `FillOrder` accounts as `fill_perp_order`) selects the router pass — vAMM + DLOB makers today, external CPI books when their execute/settle leg lands. IDL + fuzz IDLs + `velocity_idl.rs` regenerated |
| 2026-07-28 | CLOB wire ABI extended for exact velocity bookkeeping (pre-freeze): `UserBalanceChange` gains `completed_orders` (fully-consumed resting orders per user, so velocity decrements open-order counts), `RemovedOrderV0` gains `side` (which aggregate the remainder unwinds) and `cancel_order_v0` now returns it, and quote/execute args gain `taker: Option<Address>` — the taker's own resting orders are skipped unconditionally (self-trade prevention; the grace rule was the wrong tool for a user crossing their own book) | CLOB + velocity mirrors updated; CLOB tests cover completion counts + self-trade skip (13 green) |
| 2026-07-28 | — (implementation) | External quoters execute for real: the fill entrypoint quotes each passed `QuoterV0` entry via CPI (inactive/unapproved skipped, market mismatch fails), threads a `CpiQuoterExecutor` into the router pass, which validates the response (overfill, per-unit at-or-better, loaded-users-only) and settles per balance change through the new `settle_external_match_fill` (match-fee schedule, no maker `Order`; `OrderFilledWithExternalQuoter` explanation appended). CLOB fills/culls unwind the maker's open-order aggregates; Custom PropAMM fills don't (never reserved). Velocity-mediated `place_clob_order`/`cancel_clob_order` adapters land (margin gate + aggregate reserve at place, unwind at cancel, CPI as the velocity signer). litesvm integration tests drive the full path: adapter placement → `fill_perp_order_router` splitting across CLOB (real CPIs) + DLOB maker + vAMM → aggregate exactness; plus cancel round-trip with stale-ref fail-closed. CU: full 3-source router fill (CLOB quote+execute CPIs + DLOB settle + vAMM quote + margin checks) = 159,244 |
| 2026-07-28 | Clamp confirmed independent of the AMM-JIT deletion; AMM-JIT + the legacy step loop die when this ships (DLOB survives only as a router liquidity source) | Pre-execute margin clamp implemented for Custom books (`calculate_max_perp_order_size` against the quoted user, book truncated pre-split; CLOB books exempt — placement-reserved); `ExternalQuoterExecutor` gains `quoter_user`; unit test pins a thin PropAMM filling its margin-supported size with the vAMM covering the residual |
| 2026-07-28 | `amm_jit_intensity` dies with the rest of AMM-JIT — no dial on the last look. Shading is strictly LP-positive: the vAMM already wins that flow at its honest price by price priority, so filling at the rival's price instead costs no maker anything and only reprices flow the vAMM was making regardless | Shading is unconditional in `vamm_quote_levels` (the intensity gate added earlier today reverted); the 5% `LAST_LOOK_BAND` remains the only bound |
| 2026-07-29 | `/route` redesigned around simulation after reviewing `~/source/relay`'s `crank-turner`: run the real programs against a subscription-fed account cache in a pooled in-process SVM instead of decoding books off-chain. Forced by the Custom tier — a third-party quoter is an arbitrary program with no off-chain decoder, so `quote_v0` (i.e. simulation) is the only way to price it; simulating `fill_perp_order` whole also yields the split, margin clamps, at-or-better outcomes and CU from the real code. Supersedes yesterday's decode-and-reimplement `/route` sketch | Plan's Later section rewritten with the design + extraction path (`chain-source` crate out of crank-turner, git-dep from `rust/`); dlob-server stays DLOB-only; the SDK mirror's scope narrows to offline client-side estimates |
| 2026-07-29 | SDK mirror landed for the router's fill math: `math/router.ts` (split: cursor sanitization, priority tiers, pro rata, step quantization, taker-conservative notional rounding) + `math/vammLadder.ts` (ladder: swap-math rung pricing, last-look shading inside the band, step-aligned cumulatives, taker-limit cap). Split parity is transcribed case-for-case from the Rust unit tests (6 green); ladder parity is pinned against a dump the program itself emits (`ts_mirror_fixture` in router_adapter.rs) so the fixture can't drift silently | Clients can predict router fills again. Remaining SDK work: rewire `calculateTradeSlippage` / `calculateBaseAssetAmountForAmmToFulfill` onto the mirror (they still use legacy AMM-only math), which is what `order.ts` needs before its constants can be trusted |
| 2026-07-29 | Crank incentive follow-ups settled: one protocol `User` for all cranks; crank endpoints are dual-mode (keeper-owned `User` + signature as today, or protocol `User` + reservoir lamports for a program keeper) selected by which `filler` is passed rather than a flag; quote→SOL is an off-chain hot-role withdraw plus a Jupiter swap, with no on-chain conversion | Dual mode is what lets the cranks ship before relay is stood up — the existing litesvm tests keep covering the signed path. New surface is one hot-role withdraw instruction |
| 2026-07-29 | Crank incentive settled: a protocol-owned `User` receives the maker's flat removal reward, a per-market lamport reservoir on the conditions account pays keepers so relay's `assert_paid_v0` has a real fee to measure, and a hot role converts the accumulated quote back to gas. Rejected `min_payment: 0` — it passes the guard but leaves turners blind | Also removes every keeper-derived account from the executor: `KEEPER_PLACEHOLDER` substitutes one address, so a keeper-owned `User`/`UserStats` pair would have forced turners to derive PDAs and every keeper to hold a velocity `User`. Intended to generalize to liquidations |
| 2026-07-29 | Trigger lifecycle **implemented** (S5): `trigger_clob_order` + shadow-slot model. Two mechanics settled during the build: the shadow stays **untriggered** (`PlacedOnClob` bit alone marks it) so every DLOB matching path ignores it with zero new checks — a triggered-looking shadow would have been matchable twice; and freeing shadows on fill needed the CLOB execute wire to name completed orders, so `UserBalanceChange.completed_orders: u32` became `completed_order_ids: Vec<u64>`. Edge-gated re-arm is an observation crank: while `AwaitingTriggerRecross` is set, a trigger crank seeing the price on the non-trigger side clears the flag (places nothing), one seeing it through the trigger fails — on-chain can't watch the recross happen, only require it be witnessed. Oracle-offset trigger-limits rejected for now (a CLOB rest needs a fixed price) | `cancel_order` refuses placed shadows (`OrderPlacedOnClob`); bulk sweeps (cancel-all, force-cancel) skip them — their reclamation is the CLOB force-cancel extension, still open. Lifecycle litesvm test runs arm→trigger→evict→recross-gate→re-place→expire end to end |
| 2026-07-29 | Crank incentive loop **implemented** end to end: dual-mode executors (`authority` demoted from `Signer` to a payout account; `can_sign_for_user` relaxed to `can_crank_for_filler`), simulation-only resolvers staging `ResolvedCrankV0` into a scratch region on the conditions account, `place_clob_order`'s optional min-fold expiry hint, executor-side hint repair (true-min rescan of the post-removal book), and the `FeeWithdraw`-role `withdraw_protocol_user_deposit` (deposit-capped, recipient-locked). Two design deltas found during the build: a third `EverySlots` **expire-fallback condition** (the spec's own recommendation) so the hint account can stay optional on placement without a liveness hole, and — per review feedback — **conditions setup folded into `update_perp_market_clob_quoter`** (`init_if_needed`; attach = cranks stand up, re-attach = re-price), deleting the separate init ix. No protocol-User init ix either: `initialize_user`'s authority is unchecked, so the signer-authority `User` is creatable through the normal path | `ClobCrankConditionsV0` grew a 512-byte staging region (1400 bytes total). Two new litesvm tests run the whole loop the way a turner does — resolver return-data pointer → staged bytes → placeholder substitution → unsigned executor — asserting reservoir debit, protocol-`User` accrual, hint repair, and both evict sides (pinning the CLOB header/node byte offsets velocity now reads). Payout accounts must be rent-exempt after the credit; keepers use funded wallets |
| 2026-07-29 | Noted the language boundary the simulated-book decision implies: the simulation stack is Rust-only and `apps/dlob-server` is TypeScript, so book *production* has to move to Rust while book *serving* stays in TS. Also resolves a contradiction between the router-sim placement note ("dlob-server does not grow quoter awareness") and the later uniform-book decision ("dlob-server simulates every source") | The Redis publisher is the seam; see the new consequence bullet under the dlob-server decision. Two sub-decisions still open: Redis payload shape, and whether the DLOB-only path cuts over at once or runs alongside |
| 2026-07-29 | Ladder pricing corrected twice more under the TS suite: rung prices now come from `calculate_base_swap_output` (the execute leg's own math — the raw-invariant marginal diverges from what a spread market charges), and the fill no longer re-truncates the vAMM book against the taker limit (the ladder already capped it; a dust rung's notional rounds up to a whole lamport, inflating its quoted per-unit price past the limit that admitted it and costing the taker affordable liquidity). A price clamp tried in between was reverted — it over-quoted the short side | ordersWithSpread / orderMarginChecks / isolatedTransferMarginChecks / velocityClient all green; `order.ts` still failing (8) |
| 2026-07-29 | THE FLIP: the router pass is the only production fill path. `fill_perp_order` (controller) builds empty router inputs when no externals ride along, so every entrypoint — the fill ix, place-and-take flows, liquidation fills — runs quote→split→execute; the `fill_perp_order_router` ix folded into `fill_perp_order` (quoter section + baseline on the standard ix); `probe_router` + its integration test deleted. Fill-accounting fixes the flip surfaced: split notionals round toward the taker-conservative bound (ceil long / floor short) + 1 quote-lamport terminal slack in `fill_at_or_better` (the AMM's exact-notional ±1); every executed allocation floors to `order_step_size` (ladder chunks/pro-rata slices/limit caps aren't naturally aligned — non-multiples broke the market's position-counter invariant). TS suite migration in progress — 35 of 36 files pass; `order.ts` remains | Legacy step loop + JIT machinery now unreachable from production; their gut + unit-test migration is the remaining sweep. `order.ts` failures are semantic, not dust: the router fills where the legacy AMM-only path declined (its assertions encode "no liquidity at mark" via `calculateBaseAssetAmountForAmmToFulfill`), so they need per-test reconciliation **plus** the TS SDK mirror — `calculateBaseAssetAmountForAmmToFulfill`, `calculateTradeSlippage`, and a TS port of the ladder + split — before clients can predict router fills |
| 2026-07-28 | — (implementation) | Pre-flip items landed: (1) taker limit threaded into `vamm_quote_levels` as an honest final rung (total capped at the curve inversion's reachable size; rival rungs past the limit dropped) — tight-limit takers get the fillable slice instead of an empty vAMM book; (2) mandatory baseline: `PerpMarket.clob_quoter` (layout 1296→1328, appended at the tail) + `update_perp_market_clob_quoter` warm-admin ix; `fill_perp_order_router` fails unless the named entry rides the tx (a dead entry passes — killing the book never bricks fills); (3) `crank_clob_evict`/`crank_clob_remove_expired` keeper ixs: CPI the CLOB removal, fail closed if it hit a different maker than the loaded `User`, pay the flat filler reward from the maker, unwind aggregates. Oracle-gating item resolved by inspection: the router path shares the legacy pre-flight (price-band + amm validity gates) and post-fill checks (aggregate fill price vs oracle bands, stale-oracle margin/OI rules) — no router-only gap. 7 litesvm integration tests green |

## Design snapshot

Terse restatement of decided design, for cross-referencing. Numbered so tasks can cite it (S1–S7).

1. **Quoter interface**: program exposing `quote_v0`/`execute_v0`; raw 8-byte discriminators; borsh args after discriminator. Responses are written into the quoter's registered response account (owned by the quoter program); return data carries only a ResponsePointerV0 (offset, len) into it (writer must be the quoter program), so payloads are not bound by the 1024-byte return-data cap. Quote/execute args carry the set of `User`s velocity has loaded (`users: Option<Vec<Pubkey>>`, `None` = unrestricted off-chain discovery); quoters must not fill anyone else, and velocity rejects returned balance changes for unloaded users. CPI data cost is ~32 B/user and the list is bounded by the tx account budget anyway (every listed user is a writable account in the tx), so size is a non-issue. Registered in a `QuoterV0` registry entry (user, program_id, response_account, discriminators, account lists, type, is_active/is_approved). **Quoter CPIs never receive signer privilege** — every forwarded account is `is_signer: false`, because signer status propagates through CPI and an untrusted quoter handed a taker wallet could drain it. Quoters verify who signed the outer transaction (e.g. the `flow_authority` attestation) via instructions-sysvar introspection. Velocity may still sign as its own PDA with `invoke_signed` (that is velocity signing as itself, not forwarding).
2. **CLOB**: standalone program. Node arena + free list + two best-first sorted intrusive linked lists; `NIL = u32::MAX`; velocity `User` pubkey inline on the node (no seat table). Velocity price/size units; tick/step/min enforced at place, not in storage. Per-side reserved capacity; eviction is crank-mediated (decided 2026-07-28, replacing inline better-price tail eviction): `evict_worst_v0` removes the side's tail once the side is at/above `evict_threshold_per_side` (soft cap), and a full side rejects every placement — even better-priced — until the crank catches up (hard cap = ops failure; the buffer + keeper reward keep it clear). `activation_slot` speed bump (default +1 slot, may be set to 0 — dropping the bump is under consideration; max 20 = auction) — this replaces JIT. `flow_authority`-signed attestation (200–400ms API hold) bypasses the delay; quoters introspect the signer, no args flag. Expiry (`max_ts`) only skipped by quote/execute, reclaimed by the `remove_expired_v0` crank (mirrors today's keeper-cranked DLOB expiry); a partial fill leaving a remainder below `min_order_size` culls the order in the same pass, reported on the wire (`ExecuteResponseV0.cancelled`) so velocity decrements aggregates — that maker was just filled, so their `User` is loaded. Cancel by verified node hint (`OrderRef`), fails closed. Fills race the tx's fixed account set: quote/execute take the loaded-user set; a matchable order whose user is absent is skipped while at most `unknown_user_grace_slots` old (keeper couldn't have known it — also kills the grief of bricking fills with fresh top-of-book orders) and fails with `StaleUserSet` once older (keeper is stale or pruning makers; queue-jumping is bounded to the grace window, so users need not be signed into the swift message). `placed_slot` on the node feeds the age check; keepers best-effort over-allocate users past quoted depth and retry on failure.
3. **Risk**: no placement-time gating for PropAMM liquidity; validation at fill. Clamp size to margin-supported max *before* the execute CPI (can't unwind external state); categorical gates (reduce-only, liquidation, equity floor, oracle validity) at the same point; returned balance changes must be at-or-better than quoted levels. Quote-time truncation is advisory. CLOB orders keep the DLOB margin model (decided 2026-07-28; `open_order_notional` + margin-floor scheme dropped): `meets_place_order_margin_requirement` at placement, resting orders counted via the existing worst-case `open_bids`/`open_asks` machinery — every path that changes exposure is velocity-mediated — place/cancel/fill directly, sub-min culls via the execute response, evict/expiry via permissionless cranks that CPI through velocity with the maker's `User` loaded — so the aggregates are exact: no drift, no reconcile ix (dropped 2026-07-28). PropAMM depth needs no declaration (`quoting_reserve` dropped 2026-07-28 — it was only load-bearing under the margin floor): routers margin-truncate quoted levels against the PropAMM user's account (the quote-time truncation above) and the fill-time clamp enforces the same bound on-chain; overquoting only degrades that maker's fill guarantee. Fill-time clamp stays for staleness, but for CLOB makers it's the edge, not the primary defense. No new crank for deteriorated accounts: the existing `force_cancel_orders` keeper ix (controller/orders.rs:3820 — fires on initial-margin/equity-floor failure pre-liquidation, skips risk-reducing orders, flat fee from the user) extends to CLOB orders via keeper-passed `OrderRef`s. Orders stranded below a raised `min_order_size` need no sweep — they were margin-gated in and just rest (tail eviction takes them if uncompetitive; execute's cull removes sub-min remainders on partial fill).
4. **Router**: off-chain `/route` picks N quoters + accounts (advisory); on-chain router computes the split from live quote CPIs. Mandatory baseline: CLOB + vAMM always in the split. Optional signed route in the swift message: every signed quoter must be present (skip if registry-deactivated); keeper is relayer for routed orders.
5. **User orders**: plain limits live only on the CLOB. `User.orders` = conditional store (needs a trigger event to become matchable). Lifecycle `Armed → Placed` (slot keeps trigger params + CLOB `OrderRef`) `→ freed | back to Armed`. Re-arm is eager — the evict crank runs through velocity with the maker's `User` loaded, so it flips the trigger slot holding that `OrderRef` back to Armed in the same tx — and edge-triggered (price must recross). Trigger-time gate mirrors today's `trigger_order` (controller/orders.rs:3738): risk-increasing + non-reduce-only + account fails initial margin / buffered equity floor / equity breaker → cancel with `InsufficientFreeCollateral`, never re-arm (checked before the place CPI instead of after going live — same outcome). UI open orders = dlob-server index over CLOB accounts; chain is truth.
6. **Priority**: admin-assigned `priority: u8` on each registry entry (decided 2026-07-28 — replaces the CLOB-first boolean); lower fills first at a shared price, pro rata within a tier. Defaults by type: vAMM 0, CLOB 10, Custom 20 (gaps for the DLOB bridge / promotions). Price beats priority — tiers apply only within a price. Consequence: a maker must beat the vAMM by a tick to see flow at that level.
7. **Migration**: DLOB wraps as a quoter for side-by-side; JIT dies at the taker-default flip (CLOB auction replaces it); phases per Notion Migration Plan.

---

## Phase 1 — CLOB program

Standalone, tested CLOB. **Anchor v2 = the `anchor-next` alpha (git deps `anchor-lang-v2`/`anchor-v2-testing` from otter-sec/anchor). Tests in litesvm (not bankrun).**

- [x] New Anchor v2 workspace `anchor-v2/` (hosts CLOB, Midpoint in phase 3, and any future v2 programs): own `Cargo.toml` + lockfile + `Anchor.toml`, own `target/` via `.cargo/config.toml`, `exclude`d from the root workspace (mirror the `rust/` precedent)
- [x] `.so` fixture tooling: `program:build:clob`-style script builds with the v2 toolchain and copies `clob.so` to a fixtures dir the velocity workspace can load (litesvm `add_program_from_file`, needs only the `declare_id` pubkey — the deploy keypair lives OUTSIDE the repo, ~/.config/solana/velocity-keys/); CI orders the anchor-v2 workspace build before velocity integration tests
- [x] Port state from the `programs/velocity/src/state/clob.rs` sketch; delete the sketch from velocity's tree (S2)
- [x] Instructions (all V0-suffixed): `initialize_market_v0`, `update_market_v0` (admin: tick/step/min, delays, grace, evict threshold, authorities), `place_order_v0`, `cancel_order_v0`, `quote_v0`, `execute_v0`, `evict_worst_v0`, `remove_expired_v0`
- [x] `place_order_v0`: grid/min validation, activation_slot computation (default/chosen/zero), hard-cap reject (S2)
- [x] `execute_v0`: best-first consume, partial fill in place, expired skipped (crank reclaims), sub-`min_order_size` remainder cull on the wire response, `ExecuteResponseV0` via return data (S1)
- [x] Crank ixs `evict_worst_v0` (soft-cap tail eviction) + `remove_expired_v0`: velocity-mediated removal returning `RemovedOrderV0` so aggregates stay exact (S2/S3)
- [x] `quote_v0`/`execute_v0` loaded-user set: absent-user orders skipped while ≤ `unknown_user_grace_slots` old, `StaleUserSet` past it; `placed_slot` on the node (S2)
- [x] Events: place/fill/cancel/evict/expire records (evict distinct from cancel — UI + lazy re-arm depend on it, S5)
- [x] litesvm harness + tests: price-time priority, level aggregation, quote size cap, eviction (equal price rejected, per-side isolation), stale cancel hint fails closed, speed bump gates quote+execute, attestation bypass, expiry reclamation
- [x] CU benchmarks (anchor v2): place(evict, full book) 1,618 CU (was 2,690 on v1); quote(full side, 128-level cap + response write) 9,309; execute(50 orders) ~26.7k with the compact ExecuteRecord event (single event: fills as (order_id, base) pairs + expired ids); place(evict) 1,791; quote 10,220 — slab indexing adds ~10-15% vs the fixed array, in exchange for per-market capacity. Binary 92KB (v1 was 150KB). Crank-evict redesign: place(best, full book) 1,257 (no inline evict walk), evict_worst 1,042, quote(full side) 10,683, execute(50) 27,726
- [ ] Exit: `quote_v0`/`execute_v0` ABI frozen + documented; CU budget known per op

## Phase 2 — Router in velocity (CLOB + DLOB + vAMM)

Quoter registry + on-chain router inside velocity. Tests in litesvm, cross-program.

- [x] litesvm harness: standalone `integration-tests/` workspace (fuzz/e2e-svm isolation model) loading `velocity.so` + `clob.so` fixtures with clear build-first errors; `bun run test:integration[:full]`; heavy state (State/PerpMarket/User) synthesizable via `set_account` where a test needs identity, not the init flow
- [x] `QuoterV0` registry: `prop_amm.rs` promoted (gains `authority`; SIZE 2728, PDA ["quoter", market u16, program, user]). Roles: creation is consent — Custom entries must be created by the quoted `User.authority`, who keeps a permanent kill switch (`is_active`, no authority handoff); the admin vets the CPI surface (`is_approved`, validated non-empty lists containing the response account), reset by any config change. Ixs: `initialize_quoter` (born active+unapproved), `update_quoter_accounts` (chunked slice writes, clears approval), `update_quoter_config` (clears approval), `update_quoter_active` (authority), `update_quoter_approved` (warm admin); lifecycle covered end-to-end in `integration-tests/tests/quoter_registry.rs` (S1)
- [x] Quoter CPI legs proven cross-program + CU-sized: `invoke_quoter` now `invoke_signed`s as the velocity signer PDA (registered signer slot = how a quoter authenticates that velocity is the caller — the CLOB's `place_authority` is the velocity signer) and forwards the quoter program's account info; test-build-only `probe_quoter` ix (anchor-test cfg, never in devnet/mainnet .so) runs quote+execute end to end. CU over 30 CLOB orders: quote round-trip 11,738, quote+execute 33,475 — a 3-quoter fill's CPI legs ≈ 100k CU, comfortable; margin clamps are the remaining budget question (S1)
- [x] S6 split math (`math/router.rs`, unit-tested): price-round waterfall — one cached top per book per round, priority tiers ascending at each price (vAMM → CLOB → customs), pro rata within a tier at base-precision granularity (floor dust to the first with spare depth); books are untrusted `quote_v0` output, sanitized to a capped monotone best-first prefix so a garbage quoter only truncates its own book. TS SDK mirror deferred until the router fill is a real (non-test) instruction. `probe_router` (replaces `probe_quoter`) quotes N entries, splits, executes each allocation, and enforces at-or-better-than-quote per unit (cross-multiplied). 3-book CU: quote+split 25,269; +execute 49,310
- [~] **Router fill-path rewrite** (designed 2026-07-28 from a full-path audit; replaces the old "router fill ix" framing — the router integrates into `fill_perp_order`, not beside it). **Controller half done**: `fulfill_perp_order(external_books: Option<&[QuoterBook]>)` branches into `fulfill_perp_order_router_pass` (quote → split → execute+settle in one pass, `settle_dlob_match_fill`/`settle_amm_house_fill` bodies reused, external allocations rejected until CPI wiring), shared post-loop checks extracted to `fulfill_perp_order_post_checks`, `fill_perp_order` split into a thin legacy wrapper over `fill_perp_order_with_router`; router-pass unit test green. **Remaining**: ix-layer selection + external CPI book threading, at which point `probe_router` and the S7 AMM-JIT deletions fire:
  - **Dies** (collapses into one router pass): `determine_perp_fulfillment_methods` + `PerpFulfillmentMethod` + the step loop; the scratch-AMM routing projection (orders.rs ~1845-1907 — needed only because routing and quoting were separate passes); `AmmJitQuoter` + `vlp/amm/math/jit.rs` + `amm_jit_intensity` (and its admin ix) + `amm_jit_allowed` threading + `is_jit_within_match` (AMM-JIT ≡ a one-rung last-look shade at the maker's frozen price with priority tie-break — the ladder is its general form); `match_maker_price` freezing (single-pass quotes and executes off one oracle snapshot; `fill_at_or_better` + `validate_fill_price` carry the safety); the per-step fallback-price recompute; `fill_amm_only`/`match_take` after DLOB migration completes.
  - **Survives unchanged**: the per-ix pre-flight (taker+maker `settle_funding_payment`, maker sanitization cancels + flat rewards, expiry/reduce-only checks, the gate block, discovery `get_maker_orders_info` — its 16-maker cap and cancels) and post-loop (aggregate `validate_fill_price_within_price_bands` + `last_fill_price`, taker+maker margin/equity-floor block fed by `maker_fills`, `max_open_interest`, `update_funding_rate`, last-active stamps). Per-fill: the `settle_amm_house_fill`/`settle_dlob_match_fill` bodies, keyed by `fee_policy` as today.
  - **New `fulfill_perp_order_router`**, phase-structured for Solana borrows: (A) quote — copy each sanitized maker's `(price, unfilled)` out as plain data, quote external CPI books, vAMM ladder last with everything as rivals, `AmmQuoter::setup` once; (B) split — `split_across_quoters` over books truncated at the effective taker limit (`router_take` now does this); (C) execute+settle per allocation — re-borrow that maker's `User`, `RouterQuoter::execute` + the matching settle fn (`match_maker_price := fill.clearing_price`), maker_fills accumulation, deferred mark-twap on first fill, volume/open-orders bookkeeping.
  - [x] vAMM quote takes the taker limit as an *honest* ladder bound: total capped at the curve inversion's reachable cumulative (final rung exactly at the limit), rival rungs past the limit dropped.
  - Gating: the legacy path stays fully intact side-by-side (migration requirement); selection is per fill via the `fill_perp_order_router` keeper ix (same `FillOrder` accounts). External quoters ride its `remaining_accounts`: `QuoterV0` entries (detected by owner + discriminator after the map/user sections) plus the union of their registered CPI accounts — each live entry is quoted via CPI into a book, and allocations execute through `CpiQuoterExecutor` → `settle_external_match_fill` (match-fee schedule, no maker `Order`; CLOB fills/culls unwind the maker's reserved aggregates via `completed_orders` + `cancelled`). Remaining before the default flip: taker-limit as an honest vAMM rung, baseline enforcement, then the S7 AMM-JIT deletions and `probe_router`'s death (S1). Decided 2026-07-28: AMM-JIT (and the step loop) get deleted when this ships — the DLOB stays only as a router liquidity source (composability; existing makers keep working), not as a separate fill path
- [~] Fill-time validation (S3): at-or-better-than-quote enforced per external allocation (aggregate, cross-multiplied) and per internal fill; balance changes restricted to loaded users (`get_ref_mut` fails otherwise) and the taker (self-trade) excluded at the source. Pre-execute margin clamp done for Custom books: before the split (and before this market's RefMut), the pass sizes each Custom book against its quoted user via `calculate_max_perp_order_size` (initial margin, strict, isolated-aware, IMF iteration) and truncates the levels — a thin PropAMM fills what its margin covers and the residual routes onward instead of the whole fill failing at the post-check. CLOB books deliberately skip the clamp (margin-reserved at placement; the shared post-fill check is the edge). Oracle-validity gating resolved by inspection: the router path runs the same shared pre-flight (market price band, amm oracle-validity for vAMM inclusion) and post-fill checks (aggregate fill price within oracle bands, stale-oracle margin + OI rules) as the legacy loop — there is no router-only oracle read left ungated
- [x] Mandatory baseline enforcement (S4): `PerpMarket.clob_quoter` names the canonical CLOB entry (warm-admin `update_perp_market_clob_quoter`, validated Clob-typed + market-matched); `fill_perp_order_router` requires it among the passed entries — dead entries satisfy the check (passed but skipped at quote time) so killing the book never bricks fills. The vAMM half is inherent: in-program, excluded only by oracle validity
- [x] vAMM as quoter: quote leg done with **price ladder + last look** (decided 2026-07-28) — `vlp/amm/router_adapter.rs::vamm_quote_levels(amm, direction, size, step, rival_books)`. Rival prices within a 5% band of the vAMM top (`LAST_LOOK_BAND`) become rungs via `calculate_base_asset_amount_to_trade_to_price`; the slice cheaper than a rival is quoted AT the rival's price (tie won by tier priority — the shading margin is LP surplus). Beyond the last rung the curve is priced honestly by equal-size checkpoints at exact marginal prices; every rung = marginal price at its cumulative, so monotone + at-or-better with slack, and garbage rival prices can't inflate the book (unit-tested: both directions, shading, band rejection, cap, empty). Router fill ordering requirement: CPI books first, vAMM adapter last. Execute leg wired: the router pass runs `RouterQuoter::execute` on the `AmmQuoter` and settles via `settle_amm_house_fill`; AMM-JIT deletion per S7 still pending (tracked in the rewrite task)
- [x] `RouterQuoter` trait + `router_take` engine (the fill-path entry point): the trait mirrors the wire interface exactly — `quote(direction, size, rival_books) → levels` / `execute(direction, size) → QuoterFill` — decoupled from the legacy `Quoter`/`QuoterCommit` surface (which remains only for `fill_amm_only`/`match_take` and dies with them); the AMM's per-fill refresh stays its own `setup` step run before the router quotes. `DlobOrderQuoter` bridges at the CLOB tier (a resting order = one level); the vAMM implements the shaded ladder. `controller::matching::router_take` is the third fill engine beside `fill_amm_only`/`match_take`: builds internal books worst-tier-first so each quoter sees everything already quoted as rivals (generalized last look — the top-tier vAMM sees all), splits internals + external CPI books by tier, settles internal allocations via `try_fill_solo`/`commit_fill` with a per-unit-price at-or-better check (tolerant of the DLOB's maker-favored and the AMM's +1 rounding), returns external allocations for the CPI execute leg. Unit tests: AMM+DLOB+external split with commits landing; last-look shading inside the engine (vAMM takes the tie at the rival price, per-unit cost ≤ shaded price); empty. Taker settlement/fees stay with the fill controller, wired in the router fill ix. Fill account contract: the CLOB market account is a required named account on every router fill (deterministic per market, enforces the baseline), the vAMM is in-program, DLOB maker orders are optional remaining accounts during migration
- [x] DLOB as quoter: the router pass freezes each sanitized maker's `(price, unfilled)` as a single-level book at the CLOB tier and executes through `DlobOrderQuoter` + `settle_dlob_match_fill` (S7)
- [x] CLOB adapter: velocity-mediated `place_clob_order`/`cancel_clob_order` (`place_authority` = velocity signer CPI, hardcoded CLOB ix discriminators — place/cancel aren't part of the registry's quote/execute surface). Place reserves `open_bids`/`open_asks` + open-order counts and gates margin like a DLOB placement (initial in the risk scope when risk-increasing, maintenance otherwise); cancel is deliberately NOT gated on active/approved (makers must always be able to pull orders off a killed book) and unwinds from the returned `RemovedOrderV0`. Fill-path unwind rides the execute response (`completed_orders` per balance change + `cancelled` culls) (S3)
- [~] Velocity crank ixs wrapping `evict_worst_v0`/`remove_expired_v0`: `crank_clob_evict`/`crank_clob_remove_expired` (permissionless; shared body) CPI the removal, fail closed unless it hit the loaded maker `User`, pay the keeper the flat filler reward from the maker (before unwinding — an emptied position frees its slot), then unwind the aggregates. Now **dual-mode** (protocol-`User` filler selects reservoir-lamport payout, no signature; see the relay section under Later) with resolvers, conditions setup on the attach ix, the expiry-hint loop, the hot-role withdraw, and eager trigger re-arm (evict flips the shadow slot back to Armed in the same tx) all landed (S3/S5)
- [x] Trigger orders: Armed/Placed lifecycle landed — `trigger_clob_order` places a met trigger-limit on the CLOB; the `User` slot becomes a shadow (`PlacedOnClob` bit + `OrderRef` in the dead auction fields, deliberately still *untriggered* so DLOB matching ignores it; the CLOB order carries its open-order count). Freed on fill (`completed_order_ids` on the execute wire)/cull/expiry/cancel; evict re-arms eagerly, edge-gated by `AwaitingTriggerRecross` (a crank observing the price back through the trigger clears it). Margin gate mirrors `trigger_order` (cancel `InsufficientFreeCollateral`). Stop-markets stay on `trigger_order` → speed-bumped taker flow. Not yet: oracle-offset trigger-limits are rejected (no fixed resting price) (S5)
- [ ] Extend `force_cancel_orders` to CLOB orders: keeper-passed `OrderRef`s, cancel CPI + aggregate decrement, same margin gate and flat fee (S3)
- [ ] `place_and_take`: remainder placed on CLOB
- [~] litesvm tests: `router_fill.rs` covers the real `fill_perp_order_router` across CLOB (live CPIs) + DLOB maker + vAMM with aggregate exactness at place/fill, and the cancel adapter round-trip (stale ref fails closed); `router_probe.rs` covers the 3-book priority-tier split on the wire. Remaining: margin clamping (incl. risk-reducing fills at zero free collateral), baseline enforcement, sequential multi-quoter fills against shared margin, trigger lifecycle incl. evict→eager-re-arm + margin-fail cancel, cull/evict/expire aggregate exactness
- [ ] Housekeeping: Error enum append-only; `DRIFT-TO-VELOCITY.md` + admin CLI + TS SDK mirrors updated per repo conventions
- [ ] Exit: full fill flow across CLOB + DLOB + vAMM under litesvm

## Phase 3 — Midpoint PropAMM

Reference PropAMM. **Anchor v2, litesvm**, plus integration tests of everything together.

- [ ] Scaffold in the anchor-v2 workspace (+ `.so` fixture wiring like the CLOB); `MidpointQuoterV0` state (name seed, config/hot authorities, mid_price, 64 bid/ask levels with filled_size)
- [ ] `set_mid_price_v0` / `set_bid_levels_v0` / `set_ask_levels_v0` — CU-optimized hot path
- [ ] `quote_v0` / `execute_v0` (S1 ABI)
- [ ] `flow_authority` signer introspection (toxic vs attested flow quoting example)
- [ ] Register as Custom quoter; user approval flow (`is_approved`)
- [ ] Integration tests: router split across CLOB + vAMM + Midpoint; Midpoint margin clamp incl. depth truncation against the quoter's user account; attested vs unattested quoting
- [ ] Exit: end-to-end fill splitting across all quoter types in litesvm

## Later (not yet scheduled)

- Signed route field in the swift message + on-chain enforcement (S4)
- swift: hold window + attestation signing; route hint in WS payload
- **Router selection ≠ fill simulation (corrected 2026-07-29)** — simulating one `fill_perp_order` answers "what would *this* fill do", but the router's actual job is *choosing* which quoters go in the transaction, and that choice is bounded by real limits litesvm enforces faithfully: the 1232-byte packet, the static-account ceiling (64, or ~256 via ALT), and the CU budget. Past a handful of PropAMMs the whole set doesn't fit, so selection is the job and whole-fill simulation can't be the primitive — you cannot enumerate candidate subsets by simulating a fill per subset.

  The flow that follows:
  **Built 2026-07-29**: `quote_router` (+ `initialize_router_quote_buffer`). One simulated call returns per-source verified books — CLOB via a real `quote_v0` CPI, DLOB orders bridged via `find_maker_orders` (the fill path's own discovery predicate, so a book never advertises an order the fill would skip), and the vAMM ladder — in fill order, so last-look shading is already applied. Read-only: the vAMM is quoted off a copy of the AMM (`AMM: Copy`, pinned by a test), so `setup`'s projection can't move the market; the only account written is the caller's buffer. Custom books are clamped to their user's margin at quote time and flagged `clamped`. Output goes to a `RouterQuoteBufferV0`: 16 sources × a 128-level slot each (128 = `MAX_LEVELS_PER_BOOK`, the most the split will ever consume from one book, so the buffer can't hold levels no fill could route against). Per-source slots rather than one shared region, to drop offset arithmetic. Caller-created (`#[account(zero)]`) rather than `init`, because at ~33KB it exceeds the 10,240-byte CPI allocation limit — the same reason the CLOB's market account is pre-created. The 32-length serde ceiling that first forced 32-level slots is fixed properly instead: `velocity-idl-gen` now wraps any array longer than 32 in a `BigArray<T, N>` newtype with a manual serde impl, applied at every depth, so a nested `[[T; 128]; 16]` generates as `[BigArray<QuotedLevelV0, 128>; 16]`. That unblocks every future zero-copy account, not just this one. litesvm test asserts all three sources, their exact levels, and that the market is untouched. **CU: 61,268** for CLOB + DLOB + vAMM.

  Also cleaned up while here: the AMM's per-fill refresh is now the inherent `AmmQuoter::refresh`, called directly by the fill pass and the quote view. The legacy `Quoter::setup` remains only as a delegation for `fill_amm_only`/`match_take` and dies with them — live code no longer says `<AmmQuoter as Quoter>::setup`, which was the main place the vestigial trait got confused with the `RouterQuoter` infrastructure.

  1. **Quote wide.** Simulate a velocity *view* instruction that CPIs `quote_v0` across a batch of registered quoters. Velocity does the `invoke_signed` as its signer PDA, so the registry's account lists — and any quoter that gates quoting on that signer — behave exactly as at fill time. Batches are bounded by tx account limits but are free (local, parallel), so N batches cost nothing. This is the deleted `probe_router` promoted to a first-class simulation-only instruction: relay's resolver pattern applied to routing.

     **The books never travel through return data.** The 1024-byte cap can't hold one CLOB book (its response region is 8192 bytes), let alone a batch — which is exactly why the wire already puts each payload in the quoter's own `response_account` and returns only an 8-byte `ResponsePointerV0`. For a batch the router simply names those response accounts in `return_accounts` and reads them out of post-simulation account state, the same mechanism relay's resolver uses to stage a payload without landing anything. No aggregation, no cap.

     One trap: return data is **last-writer-wins per transaction**, so after a batch only the final quoter's pointer survives — the router cannot recover per-quoter offsets from it afterwards. Velocity reads each pointer immediately after its CPI (inside `invoke_quoter`), so it has them all; the view instruction must therefore write a manifest (per quoter: response account, offset, len, and whether verification clamped it) into a velocity-owned scratch account for the router to read post-sim. Logs are the wrong channel — they're size-capped and truncated.

     **Verification is the reason to route this through velocity at all**, rather than invoking quoters directly: the returned book is the *verified* book, with phantom depth already removed. Custom quoters get the pre-execute margin clamp (`calculate_max_perp_order_size` against the quoted user, already implemented in the fill pass) applied at quote time too, so a PropAMM advertising depth its account can't support is truncated before the router ever considers it. CLOB depth needs no clamp — it was margin-gated at placement.
  2. **Select narrow.** Run the split over the collected books to pick the subset that maximizes fill quality subject to one transaction's account + CU budget. `velocity-router-sim` depends on the program as a host library, so it calls the real `split_across_quoters` — no port, no mirror to drift.
  3. **Verify once.** Simulate the chosen `fill_perp_order` end to end. That's what catches the margin clamps, at-or-better rejections and the baseline check, and its CU number sizes the real transaction.

  Consequence for the SDK mirror: its job is client-side estimation, not selection. Selection is Rust, server-side, against the program's own math.

- **`/route` by simulation, not by decoding (decided 2026-07-29)** — the routing service runs the real programs against cached account state in an in-process SVM instead of reimplementing book parsing off-chain.

  The decisive constraint: **a Custom quoter is an arbitrary third-party program.** There is no general off-chain decoder for its book, so "fetch and parse the orderbook" cannot work for the tier the whole design exists to support. The only way to learn a quoter's price is to call `quote_v0` — which means simulation. It subsumes the CLOB for free (decodable, but why write the decoder) and the vAMM for free (already in velocity).

  Better still, simulate velocity's own `fill_perp_order` rather than each `quote_v0` in isolation: one simulation yields the split, the Custom margin clamps, any at-or-better rejection, baseline enforcement, and the CU number — all from the code that will actually run. A decode-and-reimplement service can only approximate that, and every approximation is a divergence bug waiting to happen.

  **Reuse `~/source/relay`'s `crank-turner`, which already is this.** Its `ChainSource` decorator stack — `LocalSimSource<CachedSource<RpcSource>>` — provides: a subscription-fed account cache (gRPC/websocket) with coverage-based freshness (accounts under a live subscription are served indefinitely, uncovered ones age-bounded, clock-sysvar updates as a feed heartbeat, ceiling revalidation in case a subscription lies); pooled `LiteSVM` instances that retain verified program ELFs (the expensive part); upgradeable-loader programdata ELF extraction; and `simulate_transaction(tx, return_accounts)` returning post-execution account state — which is exactly how a quoter's response account is read (the wire already puts the payload there and only a pointer in return data). Its `watch_programs` mode streams every account owned by named programs, so subscribing to the CLOB program + the quoter registry + each live PropAMM makes routing simulations essentially RPC-free.

  **Done 2026-07-29**: extracted as `relay-chain-source` (relay commit `5b460ef`) — `get_watch_accounts` generalized to `get_program_accounts` over transport-neutral `AccountFilter`s (translated to RPC filters, geyser filters, or evaluated locally when the cache's subscription is broader than a query), and both feeds now take `ProgramSubscription`s instead of a hardcoded registry + watch-programs list. Coverage semantics preserved: only an unfiltered subscription covers a whole program. relay's `WatchV0` filter builders live in `crank-turner::watches`; 63 relay tests green. relay now lives at **github.com/velocity-exchange/relay**, so velocity consumes the crate as a **git dep pinned by rev** (`rust/router-sim/Cargo.toml`) rather than a path — no sibling checkout required, and the simulation layer under the router only moves when the rev is bumped deliberately. To co-develop, add the `[patch."https://github.com/velocity-exchange/relay"]` path override documented in `rust/Cargo.toml` and drop it before committing.

  Where it lives here: `rust/router-sim` (`velocity-router-sim`), consumed by keep-rs — which is what builds and lands router fills, so it needs the quote to size them and pick quoters — and later by swift, already an HTTP service in the same workspace, to host `/route`. `dlob-server` keeps the DLOB/L2/L3 API, its Redis cache and its fan-out; it does NOT grow quoter awareness and does NOT simulate — see the language boundary below.

  `router-sim` so far: `router_subscriptions` (the feed set that keeps quotes off the network — a filtered registry query per market plus unfiltered subscriptions to the CLOB and each PropAMM program), `quoter_entries` (registry entries straight from the feed), and `simulate_router_fill` (run a real `fill_perp_order` and return logs + CU + post-fill account state). Registry filter constants are derived from `QuoterV0` via `offset_of!` rather than transcribed, with a test pinning them — a memcmp at a wrong offset matches the wrong accounts silently instead of failing.

  What the SDK mirror is still for: offline/client-side prediction with no service in the loop (`math/router.ts`, `math/vammLadder.ts`). Keep it and keep it faithful, but the authoritative server-side answer comes from simulation — so route *decision* logic must not grow in the SDK.

- **dlob-server books: one uniform path — simulate every source as a quoter (decided 2026-07-29, supersedes the per-source sketch)** — the server does not decode anything and has no per-source book builders. It simulates velocity's quote view instruction at a default UI size and gets price levels back for all four sources: CLOB, PropAMM, DLOB and vAMM alike. One code path, one shape.

  Why this beats decoding, beyond simplicity: **the published book becomes the fillable book.** Levels come back after velocity's verification, so depth whose maker can no longer margin it is already gone — which is a bug in today's DLOB display (it shows resting orders regardless of whether the maker's free collateral still backs them), and is unfixable off-chain without reimplementing margin. Sub-min remainders fall out the same way. It also deletes work rather than adding it: no CLOB decoder, and the bespoke DLOB book builder goes away.

  Uniformity per source: CLOB and PropAMM are registry quoters already; the vAMM is in-program and quoted by the same `(direction, size)` through `vamm_quote_levels`; DLOB resting orders bridge as single-level books via `DlobOrderQuoter`, exactly as the fill pass does — which means the maker `User`s have to be passed, so DLOB is batched over the makers the server already tracks from its user-map subscription.

  **The sources are not independent, so quote the set, not each source.** The vAMM's ladder shades against rival books (last look): quoted alone it prices better than it will inside a real fill, so publishing an independently-quoted vAMM book would advertise a price the taker won't get. The view instruction therefore quotes a whole batch in fill order — externals first, DLOB makers next, vAMM last with everything as its rivals — and returns per-source books. That is what the fill pass already does internally, so published books equal fill-time books by construction.

  Consequences to design around:
  - **The quoted size is part of the answer.** For CLOB/DLOB, levels are size-independent (resting orders) and a size just truncates depth; for vAMM/PropAMM the levels genuinely depend on it. So responses carry the size (and direction, and slot) they were quoted at, and a client wanting more than the UI default asks for a re-quote.
  - **Simulate on tick, not per request.** An L2 endpoint served by simulation per request would not survive fan-out. Simulate on slot/account-change, cache the book, serve reads from cache — the shape dlob-server already has for the DLOB.
  - **Batching stands.** Transaction account limits bind the quote simulation too, so a market's sources span several batches. They're local, parallel and RPC-free (the account feed keeps every touched account resident), so this is a non-issue for cost — only for the code that stitches batches together.
  - **One instruction, two consumers.** The same view instruction serves the UI book and the router's selection input, so there is exactly one definition of "what is available at what price" off-chain.
  - **The publisher has to move to Rust; the server does not (open, leaning yes).** The simulation stack is Rust-only — litesvm, `relay-chain-source`, and the program crate itself — and `apps/dlob-server` is TypeScript (`src/publishers/dlobPublisher.ts` builds books off the TS `DLOB` and writes them to Redis on a tick). Nothing bridges those: there is no TS binding for litesvm-with-cached-accounts, and reimplementing the quote view off-chain is exactly what the simulation decision rejected. So "dlob-server simulates every source" above is shorthand for a split we should make explicit: **book production moves to Rust, book serving stays in TypeScript.** The seam is already there — the publisher's output is Redis, so a Rust producer (in `rust/router-sim`, hosted by swift alongside `/route`, or a small `book-publisher` bin) writes the same keys on simulate-on-tick, and `dlobPublisher.ts` stops building books. dlob-server keeps the L2/L3 endpoints, cache reads, subscriber fan-out and auth — the parts that are genuinely a web service and would be a pure rewrite cost in Rust. What still needs deciding: the Redis payload shape (today's L2/L3 JSON unchanged, so consumers don't move, vs. a per-source shape the UI needs for the four-source book), and whether the DLOB-only path is cut over at the same time or runs alongside until the simulated books are trusted. Cost of getting this wrong is not small: if we leave it implicit, the first attempt at the uniform book lands in TS and dead-ends on the missing litesvm binding.
- keep-rs: relayer mode for routed orders, margin-truncated fill building, user over-allocation past quoted depth (per-market ALT of resting makers) + retry on `StaleUserSet`
- **Cross-match crank (raised 2026-07-29, design proposed, not yet built).** Nothing matches two *resting* sources against each other today: matching only happens when a router taker fills through, so a crossed book — a CLOB bid at/above a CLOB ask, or a PropAMM bidding through the CLOB's best ask — just sits. Trigger placements make CLOB×CLOB crosses routine (an aggressive stop-limit rests crossed), and PropAMM reprices make source×source crosses routine. relay's `demo-book` models the CLOB×CLOB case as its cross condition; ours should generalize to **any two sources** (vAMM excluded: its last-look ladder prices against rivals by construction, and a transiently crossed vAMM is ordinary taker arb).

  **Mechanism (Noah confirmed the framing 2026-07-29): the protocol `User` is an arb bot through relay.** The crank is a fill in which the protocol `User` is the taker on both legs: buy X at the crossed ask, sell X into the crossed bid, both through the normal router machinery so every invariant (margin clamps on PropAMM sides, at-or-better, loaded-user rules, aggregate unwinds) is reused rather than re-implemented. Net position zero; the crossed spread lands as realized PnL in the protocol `User` — the exact sink the crank incentive loop already drains — and the keeper takes reservoir lamports in program-keeper mode. **It only crosses when the take is worth it net of fees** — no fee exemption; the executor asserts `spread − both legs' fees ≥ keeper payment`, so a resolver simulation that succeeds implies profitable, which is exactly relay's predicate model (no relay changes needed). Consequence, accepted: books can rest crossed when fees are the gulf. **Scope: CLOB×CLOB and PropAMM×CLOB only** — those strand user orders (a triggered stop-limit must fill when a PropAMM quotes through it, that's the UX this exists for); PropAMM×PropAMM crosses are external-program arb someone else picks off. Remaining implementation choice: two ephemeral place-and-take legs vs. a dedicated maker-to-maker settlement that skips the middle taker.

  **Triggers as relay conditions (raised 2026-07-29, parked as a relay-spec v1 candidate).** Two blockers keep trigger discovery in keeper bots today: relay v0 has no threshold wake — `OnAccountChange` on an oracle fires every tick, so an armed order far from its trigger re-simulates constantly — and a resolver's fixed 4-account list can't enumerate users' armed orders (relay's stated non-goal: unbounded-set discovery). The candidate fix: a relay-spec `OnValueCross { account, offset, threshold, direction }` wake kind — turners index these per watched account in a priority queue, so evaluation is O(1) per oracle tick and nothing simulates until a threshold actually crosses (the off-chain priority queue Noah wants, driven by on-chain-declared thresholds) — plus a per-market trigger-conditions account whose slots velocity maintains at place/cancel/trigger. Until then, `trigger_clob_order` is cranked by bespoke keepers with their own price queues.

  **Detection is the PropAMM-reprice problem, shared with the book publisher.** A PropAMM's quote is a function of its whole registered `quote_accounts` list — the registry already names the reprice surface — so no single `OnAccountChange` watch captures "this source may have repriced"; relay-spec v0 conditions watch exactly one `(account, offset, len)`. Plan: CLOB×CLOB gets a precise `OnAccountChange` wake over the book's `best_bid`/`best_ask`; the general case gets an `EverySlots` condition whose resolver runs the quote view (`quote_router`) in simulation and stages a match if any pair crosses — hints may fire early, never late, and resolvers are free. The low-latency path is the book publisher: it already subscribes (relay-chain-source) to every registered quote account and re-simulates books on any change, so it detects crosses the moment they land and can submit the executor directly, with the poll condition as the liveness backstop. A relay-spec multi-account watch is a possible later extension; not needed to ship.

- **CLOB cranks via relay, not a bespoke keeper (decided 2026-07-29)** — relay's `demo-book` already models this market's three conditions (its own doc calls the soft-cap one "the CLOB soft-cap pattern"), and the crank ixs built 2026-07-28 are already shaped like relay executors: permissionless, fail-closed, keeper paid from the maker — which is what `min_payment` + `assert_paid_v0` assert. So the keeper work drops out in favour of condition blocks.

  **The condition block lives on a velocity account, not the CLOB's**, and both legs are velocity instructions: removal has to adjust the maker's `User` (open-order aggregates, and the reward debit), which only velocity can do, and the resolver has to stage *velocity's* account list — the maker `User`, the perp market, the filler — not the CLOB's. `target_program` = velocity, so an operator scoping a turner to velocity picks these up.
  - **Evict** (`WakeKind::OnAccountChange`): `wake_account` is the CLOB market with `wake_offset`/`wake_len` covering `bid_count`/`ask_count`. `ConditionV0` names its wake account explicitly, so a velocity-hosted condition watching foreign CLOB bytes is native to the spec — no mirroring.
  - **Expire** (`WakeKind::AtTimestamp`): needs a `wake_ts`, and velocity mediates every placement (`place_clob_order`), so it maintains the min-over-inserts hint in its own condition block as it places. Exact, and no CLOB-side change.
  - The resolver reads the CLOB account to decide whether work exists and which order (`OrderRef`) — solving the "which order expired" discovery that would otherwise be hand-rolled off-chain, since account resolution is program code and can't drift.
  - New per-market PDA to host the blocks: `QuoterV0` has only 8 bytes of padding and a `ConditionV0` is 280, so the conditions need their own account (also bounded by `MAX_RESOLVER_ACCOUNTS`).
  - **Keeper incentive: protocol-owned `User` takes the velocity-side reward, reservoir pays gas (decided 2026-07-29).** relay's `assert_paid_v0` measures the keeper's *lamport* balance, so a crank whose only incentive is quote credited to a velocity `User` cannot express a fee and turners get no signal to prioritize or decline it. `min_payment: 0` would technically pass the guard and was rejected for exactly that reason. The loop closes in three steps: the maker pays the flat removal reward as it does today, but into a **protocol-owned `User`** rather than the keeper's; the per-market conditions account holds a lamport **reservoir** and pays the keeper `keeper_payment_lamports` per crank (rent-exempt floor, failing with `InsufficientCrankReservoir` rather than underpaying, since an underpaid crank fails inside relay *after* doing the work); and a **hot role** withdraws the accumulated quote from the protocol `User` and converts it to SOL to top the reservoirs off. Makers fund gas that way — without the top-off leg the protocol subsidizes crank gas indefinitely.

    The bigger win is the account list. `KEEPER_PLACEHOLDER` substitutes exactly one address, but the executor as built takes the keeper's `User` **and** `UserStats` — PDAs derived from its authority, which a turner cannot derive and which would force every relay keeper to hold an initialized velocity `User` just to crank. Routing the reward to a protocol `User` at a fixed address removes every keeper-derived account from the executor: the staged `ResolvedCrankV0` becomes static except for the single lamport recipient, and the `can_sign_for_user(&filler, &authority)` check drops out. Keepers need only a wallet.

    **Generalizes past the CLOB.** Any crank whose incentive originates inside velocity has this shape — liquidations being the obvious next one — so the protocol-`User` + hot-role-to-gas path is worth building as a reusable pair rather than CLOB-specific plumbing.

    **Settled 2026-07-29:** *one* protocol `User` serves every crank (the reward is fungible; per-domain accounting isn't worth an account per domain). Crank endpoints stay **dual-mode**: the same instruction is callable the way it is today — keeper passes its own `User`/`UserStats`, signs for it, takes the quote reward, no lamports move — or in program-keeper mode, where the reward accrues to the protocol `User` and the caller is paid reservoir lamports instead. The mode follows from *which* `filler` is passed, so there is no flag: `filler == protocol_user` selects the lamport payout, anything else takes the existing signed path. That means relaxing today's `can_sign_for_user(&filler, &authority)` to "signs for the filler **or** the filler is the protocol `User`" — nobody can sign for the protocol `User`, since its authority is the protocol signer. No gate is needed on the lamport payout beyond the work itself: a caller only reaches it by actually removing an expired or evictable order, which is precisely the bounty we want to pay. Keeping both modes matters because it decouples shipping the cranks from standing up relay — the existing litesvm tests keep exercising the signed path unchanged.

    **Quote → SOL stays off-chain.** A hot role withdraws quote from the protocol `User` and swaps it on Jupiter out of band; no on-chain conversion, no swap CPI in the crank path. The only new instruction is a hot-role withdraw. Deliberately dumb: an on-chain auto-conversion would drag the swap path, slippage policy and an oracle into a plumbing job that a human doing a periodic swap solves for free.
- Events pipeline + order history for CLOB records
- Migration execution (S7): devnet rehearsal → mainnet additive → metrics-gated default flip → DLOB cancel-only → legacy removal
